#include <stdio.h>

void getNumbers(int *num[]){
    printf("Write down all the numbers of wich you want to find the higher (space between then), then enter:\n");
}

int main(){    
    int numbers[5]; //Can change the amount of elements.
    int times; //The amount of times that the user want to write the numbers.

    return 0;
}